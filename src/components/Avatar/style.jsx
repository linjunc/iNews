import { Button } from 'antd'
import styled from 'styled-components'

export const LoginButton = styled(Button)`
  font-size: 16px;
  font-weight: 500;
  height: 36px;
  border-radius: 8px;
  margin-left: 16px;
`
